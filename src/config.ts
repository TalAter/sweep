/** v0 stub. Signature is the stable seam — v1 grows the body without changing callers. */
// biome-ignore lint/suspicious/noEmptyInterface: v0 stub; v1 adds fields.
export interface Config {}

export function ensureConfig(): Config {
  return {};
}
