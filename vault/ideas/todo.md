# Sweep — To Do

All remaining implementation tasks and future ideas. Completed features are omitted — see `vault/` notes for architecture reference.

---

## Analysis Dialog

- [ ] When the analysis dialog opens after a user runs an installer and no LLM provider is configured, show the "no LLM provider configured" message **and** add a button to the bottom action bar that jumps to the config wizard. Blocked on both the analysis dialog and the config wizard, neither of which exists yet.
